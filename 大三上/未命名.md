%%{init: {'theme': 'base', 'themeVariables': { 'primaryColor': '#ffffff', 'edgeLabelBackground':'#ffffff', 'tertiaryColor': '#ffffff', 'mainBkg': '#ffffff', 'bordercolor': '#333333'}}}%%
graph TD
  %% Title %%
  title[<font size=5 face='Heiti' >慧教无界——面向世界建模与多模态逻辑深推的交互式备课引擎架构图</font>]:::title

  subgraph UserDisplay[<b>用户与展现层</b>]
    UserNodes[教师👤 & 管理员👥]:::user
    DisplayInterfaces[<b>展现层应用接口</b><br>个人中心 | AI备课中心<br>互动交流中心 | 视频生成中心<br>管理中心 | 资源库中心]:::output
    UserNodes --> DisplayInterfaces
  end

  subgraph PerceptionAlign[<b>感知与对齐层</b>]
    InputNodes[文本 🔤 / 语音 🎙️ / 资源文件 📄]:::input
    FeatureExtraction[多模态特征提取与对齐]:::process
    WorldModeling[统一联合世界建模 与 AvR思维链推理]:::process
    FusionPerception[多模态识别融合理解深推]:::process
    InputNodes --> FeatureExtraction
    FeatureExtraction --> WorldModeling
    WorldModeling --> FusionPerception
  end

  subgraph AgentStateMgr[<b>Agent状态管理区 (流式)</b>]
    %% Strategy Agent
    StrategyAgent[<b>策略 Agent</b><br>意图感知与理解、全局流量调度、资源选择策略]:::agent
    %% Dialogue Agent
    DialogueAgent[<b>对话 Agent</b><br>互动交流维护、双重交互、上下文状态维护]:::agent
    %% Generation Agent
    GenerationAgent[<b>生成 Agent</b><br>创意互动、动画生成逻辑、多模态课件生成]:::agent
    %% Feedback Agent
    FeedbackAgent[<b>反馈 Agent</b><br>用户行为分析、闭环反馈与迭代优化机制]:::agent
    
    StrategyAgent ==> DialogueAgent
    DialogueAgent ==> GenerationAgent
    GenerationAgent ==> FeedbackAgent
  end

  subgraph LLM_Knowledge[<b>大模型与知识增强层</b>]
    LLM_Service_H[<b>大模型服务层</b>]:::servicesub_h
    Llama3[<font size=3>Llama-3-8B-Instruct</font> 🦙]:::model
    Qwen[<font size=3>Qwen-VL</font> 🧑‍💻]:::model
    DeepSeek[<font size=3>DeepSeek-R1</font> 🐋]:::model
    SelfDeveloped[<font size=3>慧教无界自研模型</font> 🐾]:::model
    LLM_Service_H --- Llama3
    LLM_Service_H --- Qwen
    LLM_Service_H --- DeepSeek
    LLM_Service_H --- SelfDeveloped

    KnowledgeAugH[<b>知识增强与资源中心</b>]:::servicesub_h
    RAG_H[专属知识检索与增强 (RAG)]:::kb
    ParsingH[多源资料解析与融合]:::kb
    ResourcePoolH[资源库中心]:::kb
    AgentSkillsH[<font color='blue'>Agent Skills 扩展</font>]:::kb
    KnowledgeAugH --- RAG_H
    KnowledgeAugH --- ParsingH
    KnowledgeAugH --- ResourcePoolH
    KnowledgeAugH --- AgentSkillsH
  end

  subgraph DevOpsSidebar[<b>服务治理与运维区 (侧边栏)</b>]
    Ansible[Ansible <br> Ansible标志]:::devops
    Jenkins[Jenkins <br> Jenkins标志]:::devops
    Docker[Docker <br> Docker标志]:::devops
    Git[Git <br> Git部署标志]:::devops
    K8S[弹性伸缩K8S <br> K8S标志]:::devops
    SvcInfra[服务发现 & 服务注册<br>统一网关 & 远程调用]:::devops
    
    Ansible --> Jenkins
    Jenkins --> Docker
    Docker --> Git
    Git --> K8S
    K8S -.-> SvcInfra
  end

  %% Connections %%
  DisplayInterfaces ==> InputNodes
  FusionPerception ==> StrategyAgent
  
  %% Agent requests to LLM/Knowledge
  StrategyAgent -.-> LLM_Service_H
  DialogueAgent -.-> LLM_Service_H
  GenerationAgent -.-> LLM_Service_H
  FeedbackAgent -.-> LLM_Service_H
  
  StrategyAgent <--> KnowledgeAugH
  DialogueAgent <--> KnowledgeAugH
  GenerationAgent <--> AgentSkillsH

  %% Final Feedback Loop
  FeedbackAgent -.-> DisplayInterfaces

  %% DevOps control connections
  SvcInfra -.-> LLM_Service_H
  SvcInfra -.-> KnowledgeAugH
  SvcInfra -.-> DisplayInterfaces

  %% Styling %%
  classDef title fill:none,stroke:none,font-weight:bold,color:black,font-size:20px;
  classDef user fill:#e6f7ff,stroke:#1890ff,stroke-width:2px,rx:5,ry:5;
  classDef input fill:#fff1b8,stroke:#d48806,stroke-width:2px,rx:5,ry:5;
  classDef output fill:#f9f0ff,stroke:#722ed1,stroke-width:2px,rx:5,ry:5;
  classDef process fill:#e6f7ff,stroke:#1890ff,stroke-width:2px;
  classDef agent fill:#f9f0ff,stroke:#722ed1,stroke-width:2px,rx:10,ry:10;
  classDef servicesub_h fill:#f4f4f4,stroke:#333,stroke-width:1px;
  classDef model fill:#e6f7ff,stroke:#1890ff,stroke-width:2px,rx:5,ry:5;
  classDef kb fill:#fff1b8,stroke:#d48806,stroke-width:2px,rx:5,ry:5;
  classDef devops fill:#ffffff,stroke:#333,stroke-width:1px,rx:5,ry:5;